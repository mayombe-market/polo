/** Polices système uniquement — pas de chargement réseau (réseaux à latence variable). */
export const SYSTEM_FONT_STACK =
    '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif' as const
